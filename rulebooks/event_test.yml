---
- name: Debug Token Event Stream
  hosts: localhost
  sources:
    - name: K8S Token Event Stream
      ansible.eda.token:
        token: "my-secret-eda-token-123"
  rules:
    - name: log-all-events
      condition: true
      action:
        run_job_template:
          name: "Print Event Payload"
          organization: "Default"
          extra_vars:
            event: "{{ event.body }}"
